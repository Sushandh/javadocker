pipeline {
    agent any

    stages {
        stage('maven installation in javadocker') {
            steps {
                sh 'sudo dnf install maven -y'
            }
        }
        stage('package build') {
            steps {
                sh 'mvn clean package'
            }
        }  
        stage('releasing package') {
            steps {
                archiveArtifacts artifacts: 'target/*.jar', followSymlinks: false
            }
        }   
    }
}
